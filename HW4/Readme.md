# Dog Generation

1. Install requirements
    ```
    pip install -r requirements.txt
    ```
2.  Execute bash file
    ```
    ./run.sh {image_datapath}
    # image_datapath/
         - images/
         - annotations/
    ```
